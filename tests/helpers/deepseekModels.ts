/**
 * Shared DeepSeek Harness model fixtures for tests that exercise the generic
 * provider/chat machinery with a concrete model id.
 */

/** A provider-qualified model selection id. */
export const TEST_DEEPSEEK_MODEL = 'deepseek/deepseek-v4-flash';

/** The human-readable label for {@link TEST_DEEPSEEK_MODEL}. */
export const TEST_DEEPSEEK_MODEL_LABEL = 'DeepSeek V4 Flash';
